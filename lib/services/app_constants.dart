import 'dart:ui';

import 'package:flutter/material.dart';

Color mainColor = const Color(0xff53b175);
BoxDecoration boxDecorationButton =
    BoxDecoration(color: mainColor, borderRadius: BorderRadius.circular(10));
BoxDecoration decorationBorder = BoxDecoration(
    border: Border.all(
      color: const Color.fromARGB(255, 206, 203, 203),
    ),
    borderRadius: BorderRadius.circular(15));
